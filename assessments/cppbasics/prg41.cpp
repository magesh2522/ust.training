#include <iostream>
using namespace std;

int main() {
    int N;
    cout << "Enter a number: ";
    cin >> N

        if (N > 0 && (cout << 1, N > 1 && (cout << " 2", N > 2 && (cout << " 3", N > 3 && (cout << " 4", N > 4 && (cout << " 5", N > 5 && (cout << " ... " << N))))))))
        {

        }
    return 0;
}
///doubt...